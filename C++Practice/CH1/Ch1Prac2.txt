#include "stdafx.h"
#include <iostream>

// auto 예약어는 초깃값의 형식에 맞춰 선언하는 인스턴스의 형식이 자동으로 결정되는 예약어이다.
int main()
{
	int a = 10;
	int b(a);
	auto c(b); // auto 예약어를 사용했기때문에 변수 b의 형식인 integer형식으로 자동 결정된다.

	std::cout << a + b + c << std::endl; // 따라서 a = 10, b = 10, c = 10이 되어 30이 출력
	return 0;
}