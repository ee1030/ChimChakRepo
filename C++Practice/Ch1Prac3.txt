#include "stdafx.h"
#include <iostream>
using namespace std;

// new 로 배열을 동적할당 할경우 반드시 배열 형식으로 할당 해제 하여야 한다.
int main()
{
	char *arr = new char[12]; // new연산자를 이용하여 동적 할당

	arr = { "a bb ccc" };
	
	cout << arr << endl;
	delete[] arr; // 배열 형식이므로 할당 해제도 배열 형식으로


	return 0;
}